#!/bin/sh -ex

DIR=/btsync/
$DIR/btsync --config $DIR/data/config.json

NGINXDIR=/usr/share/nginx/html
nginx

while true; do
    inotifywait -r $DIR/data/mycode
    pelican $DIR/data/mycode -o $NGINXDIR || true
done

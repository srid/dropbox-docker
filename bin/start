#!/bin/sh -ex

DIR=/btsync/
$DIR/btsync --config $DIR/data/config.json

NGINXDIR=/usr/share/nginx/html
nginx

while true; do
    inotifywait $DIR/data/mycode
    set +e
    pelican $DIR/data/mycode -o $NGINXDIR
    set -e
done
